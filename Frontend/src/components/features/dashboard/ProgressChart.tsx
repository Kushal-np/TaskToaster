import Card from '../../ui/Card';

/**
 * Placeholder for a chart component (e.g., using Chart.js or Recharts).
 */
const ProgressChart = () => {
  return (
    <Card>
      <Card.Header>My Progress</Card.Header>
      <Card.Body className="h-64 flex items-center justify-center border-2 border-dashed border-gray-200 rounded-b-lg">
        <p className="text-gray-500">Progress Chart Placeholder</p>
      </Card.Body>
    </Card>
  );
};

export default ProgressChart;